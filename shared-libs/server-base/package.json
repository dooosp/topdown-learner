{
  "name": "server-base",
  "version": "1.0.0",
  "description": "Shared Express server boilerplate with error handlers and graceful shutdown",
  "main": "index.js",
  "peerDependencies": {
    "express": ">=4.0.0",
    "helmet": ">=7.0.0",
    "cors": ">=2.0.0"
  }
}
