services:
  - type: web
    name: topdown-learner
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: ACCESS_PIN
        sync: false
      - key: NODE_ENV
        value: production
